#include <iostream>

using namespace std;

string add(string A, string B){
    int[] Asize = new int[A.size()+1];
    int[] Bsize = new int[B.size()+1];
}

int main(){
    string A, B;
    cin >> A >> B;

    add(A, B);

    return 0;
}
